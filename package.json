{
  "name": "photography-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost --port 5175 --strictPort",
    "build": "vite build --emptyOutDir false",
    "preview": "vite preview",
    "photos:scan": "node scripts/generate-photo-data.mjs",
    "photos:exif": "node scripts/fill-photo-exif.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
